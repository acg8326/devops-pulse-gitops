apiVersion: v1
kind: Service
metadata:
  name: devops-pulse-backend
  labels:
    app: devops-pulse
    component: backend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: devops-pulse
    component: backend
